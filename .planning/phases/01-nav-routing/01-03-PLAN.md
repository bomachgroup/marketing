---
phase: 01-nav-routing
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/components/screens/PartnerPortalPage.tsx
  - src/routes/partner-portal.tsx
autonomous: true
requirements: [REQ-NAV-08]

must_haves:
  truths:
    - "The 'Partner work portal' / 'Assigned tasks & reports' nav items (roles ceo, mgr, bizdev, partner, influencer) render a real screen at /partner-portal instead of a blank Outlet"
    - "The screen shows partner task KPIs, the task list (from opsState.partnerTasks), an assign-task flow, and a submit-report flow that updates task reach/leads/proof/status"
  artifacts:
    - path: "src/components/screens/PartnerPortalPage.tsx"
      provides: "Partner task KPIs, task list, assign-task modal, submit-report form"
      contains: "partnerTasks"
    - path: "src/routes/partner-portal.tsx"
      provides: "/partner-portal route"
      contains: "createFileRoute('/partner-portal')"
  key_links:
    - from: "src/routes/partner-portal.tsx"
      to: "src/components/screens/PartnerPortalPage.tsx"
      via: "component: PartnerPortalPage"
      pattern: "PartnerPortalPage"
    - from: "src/components/screens/PartnerPortalPage.tsx"
      to: "src/context/StoreContext.tsx"
      via: "useStore() -> opsState.partnerTasks / setOpsState"
      pattern: "opsState"
    - from: "src/data/defaults.ts"
      to: "src/components/screens/PartnerPortalPage.tsx"
      via: "INFLUENCERS seed data feeds the partner select"
      pattern: "INFLUENCERS"
---

<objective>
Create the missing Partner Portal screen and route for the `partner-portal` nav id.

Purpose: The nav id `partner-portal` ("Partner work portal" / "Assigned tasks & reports") is referenced by 5 roles (ceo, mgr, bizdev, partner, influencer) but currently has NO route file and NO screen component — clicking it renders a blank Outlet. The HTML reference (`Bomach Os Marketing Module.html` lines 1254-1258) defines the full screen: "Influencer & External Partner Work Portal" with KPI cards, an "Assigned campaigns & deliverables" task list with an "Assign task" button, and a "Submit partner report" form (task select, reach/views, leads generated, proof link, comment). The React port already seeds the backing data (`opsState.partnerTasks` — PT-01..PT-03, and `INFLUENCERS` in defaults.ts). This plan builds the screen following the existing RealtorPortalPage pattern.

**SCOPE DECISION (D-08):** This plan is the user-vetoable scope item. Default = build. To skip, reply "drop plan 03" (nav items will be remapped to an existing screen later, or left unclickable — user's call). To remap instead of build, reply "remap partner-portal to realtor-portal".
Output: `/partner-portal` route + PartnerPortalPage; all 37 nav ids resolve to a real screen.
</objective>

<execution_context>
@C:/Users/ukaeg/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ukaeg/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-nav-routing/01-nav-routing-CONTEXT.md
@src/components/screens/RealtorPortalPage.tsx          (pattern template — same shape)
@src/routes/realtor-portal.tsx                          (route template)
@src/data/defaults.ts                                   (OPS_DEFAULT lines 507-559, INFLUENCERS line 124)
@src/components/shared/index.ts                         (KCard, Card, Table, Button, Pill, Modal exports)

<interfaces>
<!-- Verified exports the executor needs. Do not re-explore; use directly. -->

From src/context/StoreContext.tsx:
```typescript
const { opsState, setOpsState } = useStore()
// opsState is any-typed in the port; opsState.partnerTasks: Array<{
//   id: string; partner: string; type: string; title: string; objective: string;
//   due: string; fee: number; status: string; reach: number; leads: number; proof: string }>
// opsState.realtors: Array<{ id, name, ... }>
```

From src/data/defaults.ts:
```typescript
export const INFLUENCERS: Influencer[]   // Influencer { init, bg, name, handle, platform, followers, eng, niche, rate, status }
```

Shared components (from src/components/shared): `KCard { label, value, sub? }`, `Card { title, action?, children }`, `Button { variant?, size?, icon?, children, onClick, disabled? }`, `Pill { variant?, children }` (variants seen: 'p-active', 'p-qual', 'p-lost'), `Modal { open, onClose, title, size?, children }`. Import from '../shared' (same pattern as RealtorPortalPage which uses '../../shared' from src/components/screens/ — PartnerPortalPage lives IN src/components/screens/ so use '../shared').

HTML reference for the screen (lines 1254-1258):
- Banner: "Influencer & External Partner Work Portal" — "Issue clear briefs, deadlines, deliverables, tracking links and proof requirements. Partners submit reports; Bomach reviews performance before payment."
- KPI grid (partner-kpis): derive 4 KPIs from partnerTasks (e.g. Total tasks, In production, Report submitted, Fees outstanding).
- Card 1 "Assigned campaigns & deliverables": task list + "Assign task" button.
- Card 2 "Submit partner report": task select (only tasks without submitted reports), reach/views, leads generated, proof link, comment, submit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the PartnerPortalPage component</name>
  <files>src/components/screens/PartnerPortalPage.tsx</files>
  <action>
    Create `src/components/screens/PartnerPortalPage.tsx` modeled on `RealtorPortalPage.tsx` (same file shape: useState + useStore, KCard grid, Card + Table/list, Modal form, Tailwind v4 classes, iconsax-react icons, `opsState`/`setOpsState` data flow). Import shared components from `'../shared'` and `INFLUENCERS` from `'../../data/defaults'`.

    Behavior contract:
    1. **State:** `const { opsState, setOpsState } = useStore()`; `const { partnerTasks = [] } = opsState`; local state for: `showAssign` (bool), `showReport` (bool), `assignForm` ({ partner, type, title, objective, due, fee }), `reportForm` ({ taskId, reach, leads, proof, note }), `search` (string, optional task filter).

    2. **KPI row** (4 `KCard`s computed from partnerTasks): Total tasks (length), In production (status === 'In production'), Report submitted (status === 'Report submitted'), Fees outstanding (sum of `fee` for tasks not marked paid/report-submitted — sum all `fee` for tasks with status 'Assigned' or 'In production'; format as `₦${(n/1000).toFixed(0)}K` following the RealtorPortalPage money formatting).

    3. **Left card — "Assigned campaigns & deliverables"** with an action button "+ Assign task" opening the assign modal. Render each task as a bordered row (like RealtorPortalPage's commission ledger rows): title + objective on the left; partner · type · due on the right with a `Pill` for status (variant mapping: 'Report submitted' → 'p-active', 'In production' → 'p-qual', 'Assigned' → 'p-lost'). If a task has `proof`, render the proof as a small clickable link (target="_blank" rel="noreferrer").

    4. **Assign modal** (Mirrors HTML `addPartnerTask`): fields Partner (select from INFLUENCERS names + distinct `partner` names already present in partnerTasks), Type (select: Influencer, Institutional Partner, Vendor — HTML/seed values), Title (text), Objective (text), Due (date input), Fee (number). Submit validates title+partner non-empty, then `setOpsState({ ...opsState, partnerTasks: [...partnerTasks, { id: \`PT-${(partnerTasks.length + 1).toString().padStart(2, '0')}\`, partner, type, title, objective, due, fee, status: 'Assigned', reach: 0, leads: 0, proof: '' }] })` and closes the modal.

    5. **Right card — "Submit partner report"** (mirrors HTML `submitPartnerReport`): Task select (partnerTasks filtered to status !== 'Report submitted'), Reach/views (number), Leads generated (number), Report/proof link (text), Comment (textarea). Submit validates taskId + reach, then updates the task: `partnerTasks.map(t => t.id === reportForm.taskId ? { ...t, reach: Number(reach), leads: Number(leads), proof: proofLink, status: 'Report submitted' } : t)` via `setOpsState`, clears the form.

    6. Render nothing else; do NOT touch RealtorPortalPage or any shared component.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>File exists, type-checks, exports `PartnerPortalPage`; implements KPIs, task list, assign flow, report flow per the contract above.</done>
</task>

<task type="auto">
  <name>Task 2: Create the /partner-portal route and regenerate the route tree</name>
  <files>src/routes/partner-portal.tsx</files>
  <action>
    1. Create `src/routes/partner-portal.tsx` following the exact pattern of `src/routes/realtor-portal.tsx`:
       ```typescript
       import { createFileRoute } from '@tanstack/react-router'
       import { PartnerPortalPage } from '../components/screens/PartnerPortalPage'

       export const Route = createFileRoute('/partner-portal')({
         component: PartnerPortalPage,
       })
       ```

    2. Regenerate `src/routeTree.gen.ts` — the Vite plugin (TanStackRouterVite) only rewrites it when the dev server or a build runs. Order matters:
       a. Start `npm run dev` (or run `npx vite build` once — do NOT rely on plain `npx tsc --noEmit` for this; the stale generated tree will not include the new route).
       b. Grep-verify the generation: `src/routeTree.gen.ts` contains `'partner-portal'` and `PartnerPortalRouteImport`.
       c. THEN run `npx tsc --noEmit` and `npx vite build` — both must pass with the fresh tree.

    3. Do NOT hand-edit `src/routeTree.gen.ts` (it is auto-generated and marked @ts-nocheck).
  </action>
  <verify>
    <automated>grep 'partner-portal' src/routeTree.gen.ts — must match</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vite build</automated>
    <automated>agent-browser at http://localhost:5173: sign in as CEO & Founder → click "Partner work portal" in the sidebar → assert URL /partner-portal, KPI cards visible, task rows PT-01..PT-03 visible; assign a task via the modal → assert it appears in the list with status 'Assigned'; submit a report for PT-02 → assert its status becomes 'Report submitted'</automated>
  </verify>
  <done>Route exists, routeTree.gen.ts regenerated (grep-verified), tsc + vite build pass, browser flow works end-to-end. All 37 nav ids now resolve to real routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| report/assign form input → opsState.partnerTasks → rendered list | User-entered strings (title, objective, proof link, note) flow into localStorage state and are rendered as text |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-NAV-08 | Spoofing | Partner select (INFLUENCERS + existing partners) | accept | Values come from fixed seed data; free-text partner input not required |
| T-NAV-09 | Tampering | `setOpsState` writes to partnerTasks | accept | Same trust model as every other opsState mutation in the port (local demo persistence) |
| T-NAV-10 | Information disclosure | Proof links rendered as <a href> | mitigate | Render with React text interpolation only (React escapes by default); target="_blank" rel="noreferrer" on any anchor; NEVER dangerouslySetInnerHTML |
| T-NAV-SC | Tampering | package installs | N/A | No new packages installed in this plan (iconsax-react + shared components only) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npx vite build` passes
- `src/routeTree.gen.ts` regenerated and contains the new route (grep)
- agent-browser: /partner-portal renders KPIs + tasks; assign + report flows mutate the list
</verification>

<success_criteria>
- Clicking "Partner work portal" / "Assigned tasks & reports" in any role's sidebar renders the Partner Portal screen with real data (no blank Outlet).
- Assign-task and submit-report interactions persist to `opsState.partnerTasks` (localStorage) and re-render immediately.
- The phase invariant holds: every nav item `s` across all 14 roles maps to a real route.
</success_criteria>

<output>
Create `.planning/phases/01-nav-routing/01-03-SUMMARY.md` when done
</output>
