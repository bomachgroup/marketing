---
phase: 01-nav-routing
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified: []
autonomous: false
requirements: [REQ-NAV-10]

must_haves:
  truths:
    - "A human confirms the full navigation experience works: every sidebar item navigates, highlights, titles, role switches, lead search, and the partner portal"
    - "All automated checks (tsc, build, agent-browser) pass before human confirmation is requested"
---

<objective>
Final interactive verification of the wired navigation — automated browser assertions first, then a human walkthrough and approval gate.

Purpose: Plans 01-03 did the work; this plan proves REQ-NAV-10 (all 37 sidebar screens render without blanks/404s, build passes) and gives the user the visual confirmation that the port now behaves like the HTML reference. The agent runs every automated check it can first (automation-first rule); the human only confirms the interactive feel and approves the phase.
Output: A signed-off walkthrough record in the SUMMARY.
</objective>

<execution_context>
@C:/Users/ukaeg/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ukaeg/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-nav-routing/01-01-SUMMARY.md
@.planning/phases/01-nav-routing/01-02-SUMMARY.md
@.planning/phases/01-nav-routing/01-03-SUMMARY.md

NOTE: If plan 03 was vetoed (partner-portal NOT built), run this plan after 01-02 alone and skip step 6 in the walkthrough below.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Walkthrough and approval of router-driven navigation</name>
  <files>none — verification-only plan (writes .planning/phases/01-nav-routing/01-04-SUMMARY.md on completion)</files>
  <action>
    Run the full automated gate FIRST (tsc, vite build, grep gates, the 9-step agent-browser script below). Only after every automated check passes, present the human walkthrough (dev server at http://localhost:5173), collect the approval via the resume signal, and record the outcome in the SUMMARY. If any automated check fails, fix the underlying issue (reporting back to the relevant plan) and re-run before asking for human confirmation — never ask for approval with failing gates.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vite build</automated>
    <automated>grep gate: curSc absent from src/; 'partner-portal' present in src/routeTree.gen.ts (if 01-03 executed)</automated>
    <automated>agent-browser 9-step script (see how-to-verify) — all assertions green</automated>
  </verify>
  <done>All automated gates pass AND the human replies "approved" via the resume signal.</done>
  <what-built>
    The complete navigation wiring: sidebar items navigate via TanStack Router (URL + routed screen), active highlight follows the URL, role switches land on the role's first nav screen, login lands on the role's first screen, header lead search opens /leads-detail?id=<id> and pre-selects the journal lead, pipeline lead cards open the lead detail, document.title follows the route, and the partner-portal screen/route exists (unless plan 03 was vetoed).
  </what-built>
  <how-to-verify>
    The agent FIRST runs the automated gate, then asks you to confirm:

    **Automated gate (agent runs; nothing below is delegated to the human):**
    1. `npx tsc --noEmit` — zero errors.
    2. `npx vite build` — build succeeds.
    3. Grep gate: `curSc` absent from all of `src/`; `partner-portal` present in `src/routeTree.gen.ts` (if plan 03 executed).
    4. agent-browser at http://localhost:5173 executes the full script below and reports pass/fail per step.

    **Agent-browser script (assert all):**
    1. Logged out: `document.title === 'Sign in — Bomach OS'`; URL `/` redirects to `/dashboard` (login screen shows).
    2. Sign in as **Acting Coordinator** → URL becomes `/daily-execution` (role's first screen), title 'Daily execution center'.
    3. Click each of the 5 coord nav items (Daily execution, Lead control, Funnel audit, Team overview, Pipeline, Weekly report, Content calendar, WhatsApp, Analytics, Team directory, New lead, Marketing meetings — coord's full nav) → each click changes the URL to `/screen-id`, renders a non-blank screen (assert a heading or text node exists), and the clicked item carries the active background (`bg-navy text-white`).
    4. Reload at `/pipeline` → same item still active; use browser back → `/reports` renders and highlights correctly (URL-derived state).
    5. Change sidebar role select to **Partner Manager** → URL becomes `/workdesk`, sidebar re-renders with partner nav; click "Partner work portal" → `/partner-portal` renders KPIs + PT-01..PT-03 (if plan 03 executed; else skip).
    6. Header search: type a lead name → click result → URL `/leads-detail?id=...`, lead detail card shows that lead; sidebar "Lead 360 journal" → same lead pre-selected.
    7. `/pipeline` → click a lead card → `/leads-detail?id=<id>` renders.
    8. `/dashboard` → change the dashboard role select to **Analytics Officer** → URL `/revenue-command`, title 'Revenue recovery command'.
    9. Sign out → login screen; `document.title` back to 'Sign in — Bomach OS'.

    **Human confirmation (your part):**
    1. Open http://localhost:5173 in your browser (dev server running).
    2. Sign in as each of at least: Acting Coordinator, CEO & Founder, External Partner / Influencer. For each: click through 3-4 sidebar items and confirm the screen feels correct and the URL/active-highlight/title update.
    3. Confirm the mobile behavior if convenient: narrow the window (< 768px), open the hamburger menu, click a nav item, confirm the sidebar closes AND the screen navigates.
    4. Confirm you are happy with the login landing (coord → /daily-execution) and the document.title behavior.
  </how-to-verify>
  <resume-signal>
    Type "approved" to close the phase, or describe any issue (e.g. "dashboard role select doesn't navigate", "X item goes blank") and the agent will fix and re-run.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| human verification of browser behavior | No new code introduced by this plan — verification only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-NAV-11 | Spoofing | none (no code changes in this plan) | accept | Plan modifies no source files; verification-only |
| T-NAV-SC | Tampering | package installs | N/A | No new packages installed in this plan |
</threat_model>

<verification>
- All automated gates pass BEFORE the human is asked to confirm (the checkpoint requires the agent to run them first).
- The human explicitly approves via the resume signal.
</verification>

<success_criteria>
- Every automated check passes; agent-browser reports 9/9 assertions green.
- The human approves the walkthrough, confirming REQ-NAV-10 (all screens reachable and rendering, build green).
- Phase 01 is complete; `01-04-SUMMARY.md` records the outcome and any follow-ups.
</success_criteria>

<output>
Create `.planning/phases/01-nav-routing/01-04-SUMMARY.md` when done
</output>
